apiVersion: v1
kind: Service
metadata:
  labels:
    app: salesuserreact
  name: salesuserreact
  namespace: salesorder
spec:
  type: NodePort
  ports:
  - name: "salesuserreact-service"
    port: 5001
    targetPort: 3002
    nodePort: 31001
  selector:
    app: salesuserreact      